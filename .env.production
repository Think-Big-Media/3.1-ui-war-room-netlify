# 🚨 SECURITY BREACH REMEDIATED - SECRETS MOVED TO GITHUB SECRETS
# This file previously contained live production secrets - SECURITY VIOLATION
# All secrets now properly stored in GitHub Secrets per Encore Nuggets guidelines

# TEMPLATE ONLY - NO REAL SECRETS BELOW
# GitHub Actions will populate these via secrets during deployment

# Backend API URL - Will be proxied by Netlify (leave empty for relative paths)
VITE_API_URL=

# All sensitive values now stored in GitHub Secrets:
# - SUPABASE_URL, SUPABASE_ANON_KEY
# - META_APP_ID, META_APP_SECRET, META_ACCESS_TOKEN 
# - GOOGLE_ADS_CLIENT_ID, GOOGLE_ADS_CLIENT_SECRET, GOOGLE_ADS_DEVELOPER_TOKEN
# - SENDGRID_EMAIL, SENDGRID_PASSWORD
# - POSTHOG_KEY
# - OPENAI_API_KEY
# - All other API credentials

# Feature Flags (safe to keep)
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_AUTOMATION=true
VITE_ENABLE_DOCUMENT_INTELLIGENCE=true
VITE_ENABLE_REAL_API_CALLS=false

# Environment Configuration (safe to keep)
NODE_ENV=production
REACT_APP_ENV=production

# NEXT STEPS:
# 1. Add all secrets to GitHub Secrets in repository settings
# 2. Configure GitHub Actions to inject secrets during deployment
# 3. Update deployment pipeline to use GitHub Secrets
# 4. Never commit real secrets again!